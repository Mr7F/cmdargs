from setuptools import setup

setup(name='cmdargs',
      version='0.1',
      description='Parse the command line arguments with just a decorator',
      url='https://github.com/Mister7F/cmdargs',
      author='Mister7F',
      author_email='-',
      license='MIT',
      packages=['.'],
      zip_safe=False)
